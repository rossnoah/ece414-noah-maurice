#include "pico/stdlib.h"
#include "stdint.h"
#include "stdbool.h"
#include "stdlib.h"

void main()
{
    int x = -1;
    int y = -1;

    while (1)
    {

        // read touch screen here

        if (x > 0 && y > 0)
        {
            // main loop here
        }
    }
}
